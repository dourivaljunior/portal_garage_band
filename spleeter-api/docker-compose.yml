version: '3.8'

services:
  spleeter-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ../uploads:/app/uploads
      - ../downloads:/app/downloads
    environment:
      - PYTHONUNBUFFERED=1
    # Para GPU (opcional - descomente se tiver NVIDIA)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
